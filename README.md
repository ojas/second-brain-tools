# Second Brain Tools

Built in rust.

```bash

cargo test
cargo run
cargo build

target/debug/second-brain-tools ~/sync/trees/tree_sync.json > ~/sync/trees/tree_sync.txt
```

Note: crate.io token stored locally in .crate_io_token, take care.

