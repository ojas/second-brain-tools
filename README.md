# Second Brain Tools

Built in rust.

```bash

cargo test
cargo run
cargo build

target/debug/second-brain-tools ../tree_sync.json
```

Note: crate.io token stored locally in .crate_io_token, take care.

